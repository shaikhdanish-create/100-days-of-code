import random

word_list = ["camel", "aardvork", "baboon"]

chosen_word = random.choice(word_list)
print(chosen_word)

guess = input("guess a latter").lower()
print(guess)

for latter in chosen_word:
    if latter == guess:
        print("right")
    else:
        print("wrong")
